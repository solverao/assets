run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - gosec
    - revive
    - misspell
  disable-all: false

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
